using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Damageable : MonoBehaviour
{
    // 이 객체는 단지 부착된 오브젝트가 데미지를 받을 수 있는지 없는지만 판단합니다.(플레이어 제외)
    // This object only determines whether the attached object can be damaged or not. by papago(except player)
}
